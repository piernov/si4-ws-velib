image:
  name: femtopixel/kompose:latest
  entrypoint: [""]

services:
  - docker:dind

before_script:
  - apk add docker
  - docker info
  - docker login -u gitlab-ci-token -p $CI_JOB_TOKEN $CI_REGISTRY

build:
  stage: build
  script:
    - set -e
    - kompose up -v
